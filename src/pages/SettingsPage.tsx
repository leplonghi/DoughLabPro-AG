import React from 'react';import { useTranslation } from '@/i18n';import { UnitSystem } from '@/types';import { AffiliateDisclaimer } from '@/components/ui/AffiliateDisclaimer';import { useCalculator } from '@/contexts/CalculatorContext';const ChoiceButton: React.FC<{  active: boolean;  onClick: () => void;  children: React.ReactNode;}> = ({ active, onClick, children }) => (  <button    type="button"    onClick={onClick}    className={`w-full rounded-lg px-4 py-2 text-center text-sm font-medium transition-all duration-200 ease-in-out focus:outline-none focus:ring-2 focus:ring-dlp-brand focus:ring-offset-2 ${active      ? 'bg-dlp-brand text-white font-semibold shadow-md'      : 'bg-slate-200 text-slate-700 hover:bg-slate-300'      }`}  >    {children}  </button>);const SettingsPage: React.FC = () => {  const { t } = useTranslation(['settings', 'common', 'profile']);  const { unit, setUnit } = useCalculator();  const [unitSystem, setUnitSystem] = React.useState<UnitSystem>(UnitSystem.METRIC);  return (    <div className="mx-auto max-w-2xl animate-[fadeIn_0.5s_ease-in-out]">      <button        onClick={() => window.history.back()}        className="mb-6 inline-flex items-center gap-1 text-sm font-medium text-slate-500 hover:text-slate-800 transition-colors"      >        &larr; {t('common.back')}      </button>      <div className="rounded-2xl bg-white p-6 shadow-lg ring-1 ring-slate-200/50 sm:p-10">        <h1 className="text-2xl font-bold text-slate-900 mb-6">{t('general.general_settings')}</h1>        <div className="space-y-8">          {/* Unit System */}          <div>            <label className="mb-2 block text-sm font-medium text-slate-700">              {t('form.unit_system')}            </label>            <div className="grid grid-cols-2 gap-3">              <ChoiceButton                active={unitSystem === UnitSystem.US_CUSTOMARY}                onClick={() => setUnitSystem(UnitSystem.US_CUSTOMARY)}              >                {t('form.us_customary')}              </ChoiceButton>              <ChoiceButton                active={unitSystem === UnitSystem.METRIC}                onClick={() => setUnitSystem(UnitSystem.METRIC)}              >                {t('form.metric')}              </ChoiceButton>            </div>            <p className="mt-2 text-xs text-slate-500">              {t('form.unit_system_tooltip')}            </p>          </div>          {/* Default Measurement Unit */}          <div className="border-t border-slate-200 pt-6">            <label className="mb-2 block text-sm font-medium text-slate-700">{t('common:default_measurement_unit')}</label>            <div className="grid grid-cols-3 gap-3">              <ChoiceButton                active={unit === 'g'}                onClick={() => setUnit('g')}              >                Grams (g)              </ChoiceButton>              <ChoiceButton                active={unit === 'oz'}                onClick={() => setUnit('oz')}              >                Ounces (oz)              </ChoiceButton>              <ChoiceButton                active={unit === 'volume'}                onClick={() => setUnit('volume')}              >                Cups              </ChoiceButton>            </div>            <p className="mt-2 text-xs text-slate-500">{t('common:choose_your_preferred_unit_for_ingredient_measurements_in_th')}</p>          </div>        </div>      </div>      <div className="mt-8">        <AffiliateDisclaimer variant="footer" className="bg-transparent border-0 px-0" />      </div>    </div>  );};export default SettingsPage;